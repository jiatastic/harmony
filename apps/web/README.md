# Harmony Web

This workspace is reserved for the future website.

Suggested next steps:

- Add a web framework such as Next.js or Vite React.
- Keep shared UI and domain logic in `packages/` once the web app starts to overlap with desktop.
- Leave deployment-specific config inside this workspace.
